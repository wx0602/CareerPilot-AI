# 阶段计划

## 当前开发状态与责任边界

- 当前开发阶段：第一阶段。
- 当前开发者负责：任务 B（后端与数据库接口）。
- 第一阶段工作范围：登录、上传、笔试、面试、报告接口和数据库。
- 第一阶段交付物：API 文档 + 数据库。
- 除非开发者明确提出修改任务，否则后续开发只处理 B 的职责范围，不修改或代做 A、C、D 的任务部分。

### B 当前进度（2026-07-14）

- [x] FastAPI 配置、CORS、统一错误结构。
- [x] SQLite 表结构与 Alembic 首版迁移。
- [x] 登录、游客令牌、退出和训练会话接口。
- [x] 简历/JD 安全上传和 C 解析适配层。
- [x] 笔试生成、提交、错题分析和 D 适配层。
- [x] 文本面试、评分追问和简单报告接口。
- [x] 开发桩完整跑通 `technical` 模式。
- [x] 自动化测试：12 项通过，应用代码覆盖率 92%。
- [ ] 接入 C 的真实材料解析函数。
- [ ] 接入 D 的真实组卷、评分、追问和报告函数。
- [x] Python 3.11.9 环境迁移、测试和服务启动复验。
- [ ] 将接口与演示账号交给 A 联调。

状态：B 侧第一阶段基础开发已完成，团队真实能力集成与前端联调进行中。

### main 分支数据库与日志增强（2026-07-24）

- [x] SQLite 启用外键、WAL、忙等待和异常事务自动回滚。
- [x] 新增历史会话、材料和报告常用查询索引，迁移版本升级到 `20260724_0005`。
- [x] 增加请求 ID、结构化滚动日志、慢请求/慢查询记录和安全数据库错误响应。
- [x] 日志不记录请求正文、密码、Bearer Token、上传内容或 SQL 参数。
- [x] 数据库与日志专项测试 5 项通过，最新 `main` 后端全量测试 65 项通过。

### B 分支额外授权改动（2026-07-15）

- [x] 根据开发者明确授权，将职业规划页的四个空白测评入口替换为中文第三方测评链接。
- [x] 外部链接使用新标签页安全打开，并标注第三方来源与免费范围。
- [x] 旧测评占位地址重定向回职业测评列表，不将本次改动计入 A 的其他前端任务进度。

### main 分支数字人辅助功能授权改动（2026-07-20）

- [x] 普通 `job` 数字人面试复用现有摄像头画面进行本地轻量动作、姿态与面部动态分析。
- [x] 非语言结果使用严格可选 Schema 合并到现有报告 JSON，不新增表或独立接口。
- [x] 非语言分数独立展示，不计入综合分和原能力雷达图；其他面试、路演和笔试模式保持原样。
- [x] 已增加轻量前端评分测试和后端保存、校验、幂等测试；本项为开发者明确授权，不改变既有人员分工。
- [x] 2026-07-22 修复当前会话无报告时误展示旧报告的问题，并允许短时但存在有效人脸样本的回答生成基础参考评分。
- [x] 2026-07-22 非语言报告在综合分和五维反馈之外，增加有效分析时长、人脸在画比例、姿态偏离次数三个精简指标。

## 第一阶段

目标：跑通基础流程。

范围：

- 登录
- 上传简历 / JD
- 企业笔试
- 自动批改
- 错题分析
- 文本面试
- AI 追问
- 简单报告

交付：

| 人 | 任务 | 交付物 |
| --- | --- | --- |
| A | 登录页、上传页、笔试页、文本面试页、报告页 | 可点击的前端页面 |
| B | 登录、上传、笔试、面试、报告接口和数据库 | API 文档 + 数据库 |
| C | 题库、导入、简历/JD 解析、简单检索 | 题库数据 + 检索函数 |
| D | Skill、Agent、Prompt、评分逻辑 | AI 函数 + JSON 输出格式 |

## 第二阶段

目标：扩展演示效果和完整体验。

范围：

- 数字人展示
- 语音提问
- 语音回答
- 报告美化
- 历史记录
- 部署演示

交付：

| 人 | 任务 | 交付物 |
| --- | --- | --- |
| A | 数字人页面、语音录制、音频播放、报告美化 | 数字人交互页面 |
| B | ASR/TTS 接口、历史记录接口、部署 | 语音接口 + 部署地址 |
| C | 优化题库标签、RAG、解析 PPT/计划书 | 更准的检索结果 |
| D | 优化追问、评分、报告、模式切换 | 完整报告 + 模式切换 |
